# Migration `20200820192733-added-review`

This migration has been generated by felix1234567890 at 8/20/2020, 9:27:33 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE `emp`.`profiles` (
`id` int  NOT NULL  AUTO_INCREMENT,
`filename` varchar(191)  NOT NULL ,
`userId` int  NOT NULL ,
UNIQUE Index `profiles_userId_unique`(`userId`),
PRIMARY KEY (`id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

CREATE TABLE `emp`.`Review` (
`text` varchar(191)  NOT NULL ,
`userId` int  NOT NULL ,
`bookId` int  NOT NULL ,
PRIMARY KEY (`userId`,`bookId`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci

ALTER TABLE `emp`.`profiles` ADD FOREIGN KEY (`userId`) REFERENCES `emp`.`users`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `emp`.`Review` ADD FOREIGN KEY (`userId`) REFERENCES `emp`.`users`(`id`) ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE `emp`.`Review` ADD FOREIGN KEY (`bookId`) REFERENCES `emp`.`books`(`id`) ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200820101052-added-book-model..20200820192733-added-review
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -18,22 +18,37 @@
   married Boolean?
   role Role @default(USER)
   createdAt DateTime @default(now())
   books Book[]
+  profile Profile?
+  reviews Review[]
   @@map(name:"users")
 }
+model Profile {
+  id Int @default(autoincrement()) @id
+  filename String
+  userId Int
+  user User @relation(fields: [userId], references:[id])
+  @@map(name:"profiles")
+}
 model Book {
   id Int @default(autoincrement()) @id
-  title String
+  title String 
   authorId Int
   author User @relation(fields: [authorId],references: [id])
   description String
   yearPublished Int?
+  reviews Review[]
   @@map(name:"books")
 }
-// model Review {
-//   id Int @default()
-// }
+model Review {
+  @@id([userId, bookId])
+  text String 
+  userId Int
+  user User @relation(fields: [userId], references: [id])
+  bookId Int
+  book Book @relation(fields: [bookId], references: [id])
+}
 enum Role {
   USER
   ADMIN
 }
```


