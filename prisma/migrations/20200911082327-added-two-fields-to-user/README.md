# Migration `20200911082327-added-two-fields-to-user`

This migration has been generated by felix1234567890 at 9/11/2020, 10:23:27 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE `emp`.`users` ADD COLUMN `resetPasswordToken` varchar(191)  ,
ADD COLUMN `resetPasswordExpire` varchar(191)  

DROP TABLE `emp`.`migrations`
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200824100540-change-table-name..20200911082327-added-two-fields-to-user
--- datamodel.dml
+++ datamodel.dml
@@ -2,9 +2,9 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "mysql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
@@ -18,19 +18,13 @@
   married Boolean?
   role Role @default(USER)
   createdAt DateTime @default(now())
   books Book[]
-  profile Profile?
   reviews Review[]
+  resetPasswordToken String?
+  resetPasswordExpire String?
   @@map(name:"users")
 }
-model Profile {
-  id Int @default(autoincrement()) @id
-  filename String
-  userId Int
-  user User @relation(fields: [userId], references:[id])
-  @@map(name:"profiles")
-}
 model Book {
   id Int @default(autoincrement()) @id
   title String 
   authorId Int
@@ -39,17 +33,24 @@
   yearPublished Int?
   reviews Review[]
   @@map(name:"books")
 }
+model Profile {
+  id Int @default(autoincrement()) @id
+  filename String
+  userId Int
+  user User @relation(fields: [userId], references:[id])
+  @@map(name:"profiles")
+}
 model Review {
   @@id([userId, bookId])
-  text String 
+  text String
   userId Int
   user User @relation(fields: [userId], references: [id])
   bookId Int
   book Book @relation(fields: [bookId], references: [id])
   @@map(name:"reviews")
 }
 enum Role {
-  USER
+  USER 
   ADMIN
 }
```


